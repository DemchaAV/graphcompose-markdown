package io.github.demchaav.markdown.model;

/**
 * A thematic break — the horizontal rule produced by {@code ---}, {@code ***}
 * or {@code ___}.
 */
public record ThematicBreakNode() implements MarkdownNode {
}
