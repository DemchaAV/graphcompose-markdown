# Inline formatting

This sentence has **bold text**, *italic text*, ***bold italic text***,
~~struck text~~, `inline code`, and a [labelled link](https://example.com) so a
single paragraph exercises every inline run the parser can produce.
